../demorepo2

# How to add local repo into git repository
    Terminal $ git init
    Commit all files
    Create empty git repository on github
    Terminal $ git remote add origin sshlink
    git remote -v
    git push origin main
